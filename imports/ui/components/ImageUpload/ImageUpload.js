import React from "react";
import ImageArray from '../ImageArray/ImageArray';

var frontStyle = [];

const image = function(ImageArray) {
  for (var i = 0; i < array.length; i++) {
    frontStyle.push({
      backgroundImage: "url(" + i + ")",
      borderRadius: '25px'
    });
  }
}




export default frontStyle;